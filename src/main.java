/**
 * Created by Wilhelm on 2017-09-13.
 */
public class main {

    public void intepretMessage(){
        switch(PDU.getCode) {
            case 1:
                return makeMESS();
        }

        private byte[] messageCreator() {
            return String(username + ":" + message);
        }

        private byte[] leaveCreator() {
            return String("User " + username + " has left.");
        }
    }
}
